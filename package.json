{
  "name": "acc-db",
  "version": "0.1.0",
  "private": false,
  "description": "ACC DB persistence layer for schemas, migrations, task state, workflow runs, and agent registry data.",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.13.10",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.2"
  }
}